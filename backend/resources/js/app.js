// Import Alpine from Livewire (don't import separately to avoid duplication)
// Livewire provides Alpine automatically

// If you need to extend Alpine, do it before Livewire loads
// document.addEventListener('livewire:init', () => {
//     // Custom Alpine extensions here
// });
